[project]
name = "feature_extraction_server-models-tesseract"
version = "0.1.0"
description = "tesseract model for FES."
authors = [
    {name = "Fynn Faber", email = "fynn.f.faber@proton.me"}
]
requires-python = ">=3.11"

dependencies = [
    "pytesseract==0.3.10",
    "pillow==10.3.0"
]

[tool.flit.module]
name = "feature_extraction_server.models.tesseract"

[build]
script = "flit build"
